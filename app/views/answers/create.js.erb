<% if @answer.errors.present? %>
  $('.answer-errors').html('<%= j render 'shared/errors', resource: @answer %>');
  setTimeout("$('.answer-errors ul').remove();", 3000);
<% else %>
  $('.answers').append('<%= j render @answer %>');
  $('textarea#answer_body').val('');
<% end %>
