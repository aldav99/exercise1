<%- if @answer.errors.present? %>
  $('.errors').html('<%= j render 'shared/errors', resource: @answer %>');
  setTimeout("$('.errors ul').remove();", 3000);
<%- else %>
  $('.answers').append('<%= j render @answer %>');
  $('textarea#answer_body').val('');
<%- end %>
