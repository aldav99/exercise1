<%- if @answer.persisted? %>
  $('.answers').html('<%= j render @answer %>');
  $('#answer_body').val('');
<%- else %>
   $('.answers').html('<%= j render 'shared/errors', resource: @answer %>');
<%- end %>