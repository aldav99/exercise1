<%- if @answer.persisted? %>
  $('.answers').html('<%= j render 'questions/answers' %>');
  $('#answer_body').val('');;
<%- else %>
   $('.answers').html('<%= j render 'shared/errors', {resource: @answer } %>');
<%- end %>